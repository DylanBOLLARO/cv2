services:
    cv:
        container_name: cv
        ports:
            - '3010:3010'
        build:
            context: .
        restart: unless-stopped
        volumes:
            - ./public/CV.pdf:/app/public/CV.pdf
        env_file:
            - .env
