services:
    cv:
        container_name: cv
        build:
            context: .
        restart: unless-stopped
        volumes:
            - ./public/CV.pdf:/app/public/CV.pdf
        env_file:
            - .env
